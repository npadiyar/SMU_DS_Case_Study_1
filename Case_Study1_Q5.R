quantile(sort_merged_V5_Y_NA_sub$V5.y, probs = seq(0, 1), na.rm=FALSE)

quantile(sort_merged_V5_Y_NA_sub$GDPRanking, probs = seq(0, 1), na.rm=FALSE)

quantile(sort_merged_GDP_NA_sub$GDPRanking, probs = seq(0, 1), na.rm=FALSE)

quantile(data$fpkm, prob = seq(0, 1, length = 11), type = 5

quantile(sort_merged_GDP_NA_sub$GDPRanking, prob = seq(0, 1, length=11), type=5